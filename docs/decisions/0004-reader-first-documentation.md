# ADR 0004: Keep the beginner path reader-first

User-facing guides lead with a goal, prerequisites, copyable steps, expected
results and recovery guidance. The shortest local-preview path stays separate
from optional release machinery, unavoidable terms are defined, and general
documentation advice works beyond Pascal while Pascal remains the primary
example. Technical reference and project internals remain available after the
learning path instead of interrupting it.
