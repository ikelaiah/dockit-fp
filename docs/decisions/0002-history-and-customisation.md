# ADR 0002: Immutable history and semantic customisation

Published versions render from immutable Git references, including legacy trees.
Projects own content and two accent tokens; DocKit-FP owns the responsive shell
and CSS. This preserves historical accuracy and prevents normal CSS forks.
