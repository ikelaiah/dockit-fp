# v0.6.0 task list

- [x] Add homepage cards and section-switch validation.
  - Acceptance: optional configuration preserves v0.5.0 defaults; invalid
    paths identify the exact field and correction.
  - Verify: focused configuration tests.

- [x] Render safe, configurable homepage sections.
  - Acceptance: cards retain order, values are escaped, and switches affect
    only the configured home page.
  - Verify: focused build tests.

- [x] Publish homepage recipes.
  - Acceptance: library, application, and API-reference examples are
    copyable and pass documentation validation.
  - Verify: `python -m dockit_fp check --root .`.

- [ ] Prepare and review v0.6.0.
  - Acceptance: changelog, roadmap, version manifest, test suite, and
    release checks are current.
  - Verify: full suite, `check-release`, and `build-all`.
