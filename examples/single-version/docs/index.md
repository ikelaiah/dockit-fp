# Single-version documentation

This release-ready example publishes the current `main` documentation at the
site root. It does not require `versions.json` or release tags.

Run `dockit-fp check` and `dockit-fp build` locally before pushing.
